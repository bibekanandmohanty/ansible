---
 - hosts: all
   become: yes
   tasks:
   - yum: 
        name: wget 
        state: present
 - hosts: group1
   become: yes
   tasks:
   - yum: name=httpd state=present
   - yum: name=vim state=present
 - hosts: group2
   become: yes
   tasks:
   - yum: name=httpd state=present
   - yum: name=vim state=present
